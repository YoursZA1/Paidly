# ===========================================
# Frontend environment variables (Vite)
# ===========================================
# Copy to .env and fill in, or use environment-specific files:
#   .env.development.example → .env.development   (DEV – npm run dev)
#   .env.production.example  → .env.production    (PROD – npm run build)
# Only variables prefixed with VITE_ are exposed to the client.
# Full setup: docs/SUPABASE_SETUP_AND_MAINTENANCE.md

# Backend API base URL (optional; default: http://localhost:5179)
VITE_SERVER_URL=http://localhost:5179

# Production only: set to 1 if you deploy without the Node API (silences missing VITE_SERVER_URL warning on Vercel).
# VITE_SUPABASE_ONLY=1

# Supabase (required for auth and storage)
# Use ANON (public) key only. Service role key must NEVER be exposed on frontend (server-only).
# DEV: use dev/staging project. PROD: set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY correctly in production.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Supabase Storage bucket name (optional; default: invoicebreek)
VITE_SUPABASE_STORAGE_BUCKET=invoicebreek

# Main site → app redirect: when users sign in/sign up on the main site (e.g. paidly.co.za/Auth.html), send them to the app dashboard.
# Marketing site only: VITE_APP_URL=https://www.app.paidly.co.za (canonical app host; match vercel.json redirect). Leave unset on the app deployment.
# Use the same VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY on both app and marketing site so they share the same database.
# Add https://paidly.co.za and https://www.paidly.co.za to Supabase Auth → Redirect URLs. See docs/AUTH_AND_LOGIN.md.
VITE_APP_URL=
