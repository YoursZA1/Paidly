/** Public conversion page — marketing / apply */
export const AFFILIATE_LANDING_PATH = "/affiliate";

/** Public application form */
export const AFFILIATE_APPLY_PATH = "/affiliate/apply";

/** In-app affiliate hub — referral link & stats (requires auth) */
export const AFFILIATE_DASHBOARD_PATH = "/dashboard/affiliate";
