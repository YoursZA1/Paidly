/**
 * @deprecated Import from `@/utils/apiRequest` instead (`safeFetch` or `apiRequest`).
 * Re-export preserved for existing imports.
 */
export { safeFetch, apiRequest, apiRequestJson } from "@/utils/apiRequest";
