/**
 * Default Supabase Storage bucket for general/org files when env is unset.
 * If your project still uses the legacy bucket name, set VITE_SUPABASE_STORAGE_BUCKET (and server SUPABASE_STORAGE_BUCKET) accordingly.
 */
export const DEFAULT_STORAGE_BUCKET = "paidly";
