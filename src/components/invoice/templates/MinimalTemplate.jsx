import React from "react";
import UnifiedInvoiceTemplate from "./UnifiedInvoiceTemplate";

export default function MinimalTemplate(props) {
  return <UnifiedInvoiceTemplate variant="minimal" {...props} />;
}
