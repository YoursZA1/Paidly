/**
 * Vercel serverless: POST /api/send-email (invoice/quote HTML email).
 * Implementation: server/src/sendEmailApi.js (shared with Express).
 */
import sendEmailHandler from "../server/src/sendEmailApi.js";

export default sendEmailHandler;
